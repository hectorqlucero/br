(ns br.enhanced.tabgrid
  "Minimal stub to satisfy classpath during `lein check`."
  (:require [clojure.string :as str]))

(defn init
  "No-op initializer for tabgrid (stub)."
  []
  nil)

(defn render-enhanced-tabgrid
  "Minimal placeholder implementation so routes referring to enhanced tabgrid compile.
  Accepts any args and returns a simple HTML string placeholder."
  [& _]
  "<div class=\"enhanced-tabgrid-placeholder\">TabGrid placeholder</div>")
